from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Building ML model to predict if a rookie player will last at least 5 years in the league based on his stats.',
    author='Adv DSI Group 4',
    license='MIT',
)
